# Client-side form endpoints (Google Apps Script Web App URLs)
VITE_CONTACT_FORM_ENDPOINT="https://script.google.com/macros/s/CONTACT_WEB_APP_ID/exec"
VITE_TUTOR_FORM_ENDPOINT="https://script.google.com/macros/s/TUTOR_WEB_APP_ID/exec"

# Notes:
# - Copy this file to .env and replace values for local dev.
# - For production (Vercel/Netlify), set the same keys in project env settings.
